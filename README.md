# ECG-Signal-Classification
This project uses data from KaggleHub. I build model using Convolutional Neural Network (1D-CNN) with Temporal Convolutional Network (TCN). Then I use training data and implement my model on FPGA-SoC (KV260 & KR260).
